spring:
  application:
    name: chatbot
auth:
    openai:
      api-key: OPENAI_VALID_API
    app:
      api-key: APP_VALID_API
openai:
  model: gpt-3.5-turbo
  url: https://api.openai.com/v1/chat/completions
  context: AI_STRING_CONTEXT
cors:
  allowed-origins: http://localhost:5173